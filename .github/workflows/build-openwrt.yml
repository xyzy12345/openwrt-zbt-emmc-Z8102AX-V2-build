name: Build OpenWrt for ZBT-Z8102AX (eMMC Base)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
    - name: 1. Checkout OpenWrt source
      uses: actions/checkout@v4
      with:
        repository: openwrt/openwrt
        ref: v24.10.4  # ✅ 注意：必须是 v24.10.4，不是 openwrt-24.10.4！

    - name: 2. [CORE] Patch for eMMC version
      run: |
        # 进入源码目录
        cd ${{ github.workspace }}
        
        # 先检查目录结构
        echo "=== 检查目标目录结构 ==="
        find target/linux/mediatek/ -name "*zbt*" 2>/dev/null || echo "未找到zbt相关文件"
        echo "=== 检查filogic目录 ==="
        ls -la target/linux/mediatek/filogic/ 2>/dev/null || echo "filogic目录不存在"
        echo "=== 检查mt7981文件 ==="
        find target/linux/ -name "mt7981.mk" 2>/dev/null || echo "未找到mt7981.mk"
        
        # 根据找到的文件路径进行修改
        if [ -f "target/linux/mediatek/filogic/image/mt7981.mk" ]; then
          FILE_PATH="target/linux/mediatek/filogic/image/mt7981.mk"
        elif [ -f "target/linux/mediatek/mt7981/image/mt7981.mk" ]; then
          FILE_PATH="target/linux/mediatek/mt7981/image/mt7981.mk"
        else
          echo "❌ 找不到mt7981.mk文件"
          exit 1
        fi
        
        echo "找到配置文件: $FILE_PATH"
        
        # 备份原文件
        cp "$FILE_PATH" "${FILE_PATH}.backup"
        
        # 修正eMMC启动参数
        sed -i '/define Device\/zbtlink_zbt-z8102ax/,/^endef/ {
          /KERNEL_CMDLINE/d
          /^endef/i \  KERNEL_CMDLINE := earlycon=uart8250,mmio32,0x11002000 console=ttyS0,115200n8 root=PARTLABEL=rootfs rootfstype=squashfs,f2fs
        }' "$FILE_PATH"
        
        echo "✅ eMMC启动参数已注入到 $FILE_PATH"

    # ... 后续步骤保持不变 ...name: Build OpenWrt for ZBT-Z8102AX (eMMC Base)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 180

    steps:
    - name: 1. Checkout OpenWrt source
      uses: actions/checkout@v4
      with:
        repository: openwrt/openwrt
        ref: openwrt-24.10.4  # 使用稳定版本！

    - name: 2. [CORE] Patch for eMMC version
      run: |
        # 进入源码目录
        cd ${{ github.workspace }}
        
        # 先检查文件是否存在
        echo "检查目录结构..."
        find . -name "*zbt*" -type f | head -20
        ls -la target/linux/mediatek/
        
        # 根据你查到的官方路径，设备应该在 filogic 目录
        # 备份原文件
        cp target/linux/mediatek/filogic/image/mt7981.mk target/linux/mediatek/filogic/image/mt7981.mk.backup
        
        # 修正eMMC启动参数
        sed -i '/define Device\/zbtlink_zbt-z8102ax/,/^endef/ {
          /KERNEL_CMDLINE/d
          /^endef/i \  KERNEL_CMDLINE := earlycon=uart8250,mmio32,0x11002000 console=ttyS0,115200n8 root=PARTLABEL=rootfs rootfstype=squashfs,f2fs
        }' target/linux/mediatek/filogic/image/mt7981.mk
        
        echo "✅ eMMC启动参数已注入"

    - name: 3. Update and install feeds
      run: |
        cd ${{ github.workspace }}
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: 4. Apply minimal configuration
      run: |
        cd ${{ github.workspace }}
        # 生成默认配置
        make defconfig
        
        # 应用精简配置 - 使用正确的目标
        echo "CONFIG_TARGET_mediatek=y" > .config
        echo "CONFIG_TARGET_mediatek_filogic=y" >> .config
        echo "CONFIG_TARGET_mediatek_filogic_DEVICE_zbtlink_zbt-z8102ax=y" >> .config
        
        # eMMC基础支持
        echo "CONFIG_PACKAGE_kmod-mmc=y" >> .config
        echo "CONFIG_PACKAGE_kmod-fs-f2fs=y" >> .config
        
        # 基本系统
        echo "CONFIG_PACKAGE_luci=y" >> .config
        echo "CONFIG_PACKAGE_luci-proto-ppp=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-opkg=y" >> .config
        
        # 网络基础
        echo "CONFIG_PACKAGE_dnsmasq=y" >> .config
        echo "CONFIG_PACKAGE_firewall4=y" >> .config
        echo "CONFIG_PACKAGE_opkg=y" >> .config
        
        # 应用配置
        make defconfig

    - name: 5. Download packages
      run: |
        cd ${{ github.workspace }}
        make download -j$(nproc)
      timeout-minutes: 60

    - name: 6. Build firmware
      run: |
        cd ${{ github.workspace }}
        make -j$(nproc) V=s
      timeout-minutes: 120

    - name: 7. Upload firmware artifact
      uses: actions/upload-artifact@v4
      with:
        name: openwrt-zbt-z8102ax-emmc-base
        path: ${{ github.workspace }}/bin/targets/mediatek/filogic/
        retention-days: 30
